# CloudFront Multi-Origin Configuration
# Use relative paths for seamless routing through CloudFront
# CloudFront routes /ai/* to API Gateway and /api/* to ALB

# AI Assistant API (via CloudFront /ai/* origin)
VITE_AI_API_URL=/ai/ask

# CMDB API (via CloudFront /api/* origin)
VITE_API_URL=/api

# Other configurations
VITE_APP_NAME=CMDB AI Assistant
VITE_APP_VERSION=1.0.0
